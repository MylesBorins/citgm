'use strict';

// Local modules
var test = require('./test');
var install = require('./install');
var rebuild = require('./rebuild');

module.exports = {
  install: install,
  rebuild: rebuild,
  test: test
};
